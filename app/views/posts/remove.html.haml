- title { "#{t('activerecord.models.post').capitalize} : #{t(:remove)}" }
%h2= tws :remove_confirmation
- form_for @post, :html => { :method => 'delete' } do |f|
  = link_to t(:cancel), @post
  = submit_tag t(:yes)
